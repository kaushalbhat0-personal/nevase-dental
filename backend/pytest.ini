[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    postgres: integration tests that require a real PostgreSQL database (set PYTEST_DATABASE_URL)
filterwarnings =
    ignore::DeprecationWarning
